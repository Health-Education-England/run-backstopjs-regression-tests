{
  "name": "run-backstopjs-regression-tests",
  "version": "1.0.0",
  "description": "HEE backstopjs regression testing implementation.",
  "scripts": {
    "serve": "node scripts/serve.js",
    "test": "npm run backstop:generate-config && npm run backstop:test",
    "backstop:ref": "npm run backstop:generate-config && start-server-and-test serve http://localhost:3000 'npx backstop --config=./backstop.json reference --docker'",
    "backstop:test": "npm run backstop:generate-config && start-server-and-test serve http://localhost:3000 'npx backstop --config=./backstop.json test --docker'",
    "backstop:generate-config": "node scripts/generateConfig.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Health-Education-England/run-backstopjs-regression-tests.git"
  },
  "author": "https://github.com/nhsuk/",
  "license": "MIT",
  "devDependencies": {
    "backstopjs": "^6.2.1",
    "dotenv": "^16.1.1",
    "glob": "^10.2.6",
    "http-server": "^14.1.1",
    "start-server-and-test": "^2.0.0"
  }
}
